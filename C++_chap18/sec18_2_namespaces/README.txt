subprobject 1:
It shows that each namespace is a scope
And, namespaces can be discontiguous.
We defined the primer namespace.


subproject 2:
inline namespace v.s. non-inline namespace
Unlike ordinary namespaces, names in an inline namespace can be
used as if they were direct members of the enclosing namespac.e


subprobject 3:
In subobject 3, static global member or function have a similar behavior on across-file
visibility.  It suppress the multi-definitions successfully.

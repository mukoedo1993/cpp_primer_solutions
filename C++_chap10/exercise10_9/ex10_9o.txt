The sorted words are:
3; A; Even; Even; For; I; It; a; a; a; a; a; also; an; and; and; and; and; are; as; as; ate; be; both; but; characters; comprised; considered; contain; contain; correctly; could; data; data; enclose; example; for; hamburgers; if; in; in; integer; integer; is; is; is; marks; must; name; not; number; numbers; numbers; of; of; or; phrase; phrase; point; programmers; programming; quotation; rather; recognized; represent; set; spaces; specified; string; string; string; strings; strings; such; text; than; that; the; the; the; to; to; type; unit; used; used; variable; word; 
After put all repeated words at the end:
3; A; Even; For; I; It; a; also; an; and; are; as; ate; be; both; but; characters; comprised; considered; contain; correctly; could; data; enclose; example; for; hamburgers; if; in; integer; is; marks; must; name; not; number; numbers; of; or; phrase; point; programmers; programming; quotation; rather; recognized; represent; set; spaces; specified; string; strings; such; text; than; that; the; to; type; unit; used; variable; word; ; ; ; ; ; ; string; string; ; strings; ; ; ; ; ; the; the; ; to; ; ; ; used; ; ; 
Then,delete all repeated words:
3; A; Even; For; I; It; a; also; an; and; are; as; ate; be; both; but; characters; comprised; considered; contain; correctly; could; data; enclose; example; for; hamburgers; if; in; integer; is; marks; must; name; not; number; numbers; of; or; phrase; point; programmers; programming; quotation; rather; recognized; represent; set; spaces; specified; string; strings; such; text; than; that; the; to; type; unit; used; variable; word; 

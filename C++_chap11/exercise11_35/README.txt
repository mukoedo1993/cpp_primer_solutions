Compared to the example given in the textbook, we could see that:
using trans_map[key]=substr(1) will record the value we enter at the last time.
But, using trans_map.insert(key,value.substr(1)) will record the value we enter at the first time.
